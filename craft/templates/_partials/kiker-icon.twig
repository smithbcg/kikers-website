{% set extraClass = extraClass|default('') %}
<img
  class="kiker-icon kiker-icon--{{ name }}{% if extraClass %} {{ extraClass }}{% endif %}"
  src="{{ siteUrl }}assets/icons/kiker-{{ name }}.svg"
  alt=""
  aria-hidden="true"
  loading="lazy"
>
